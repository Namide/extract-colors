{
  "name": "extract-colors",
  "version": "1.1.23",
  "description": "Extract color palettes from images",
  "scripts": {
    "prebuild": "rm -rf ./lib",
    "build": "tsc --project tsconfig.browser.type.json && vite build --config vite.config.browser.ts && tsc --project tsconfig.node.type.json && vite build --config vite.config.node.ts",
    "postbuild": "cp lib/extractColors.browser.d.ts lib/extract-colors.browser.es.d.ts && mv lib/extractColors.browser.d.ts lib/extract-colors.browser.umd.d.ts &&  cp lib/extractColors.node.d.ts lib/extract-colors.node.es.d.ts && mv lib/extractColors.node.d.ts lib/extract-colors.node.cjs.d.ts",
    "lint": "eslint src --fix",
    "test": "vitest --config vite.config.test.ts",
    "coverage": "vitest run --coverage --config vite.config.test.ts",
    "loop": "for file in lib/*/*/*.js; do terser $file --compress --mangle --mangle-props --source-map includeSources --output $file; done"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Namide/extract-colors.git"
  },
  "keywords": [
    "color",
    "tool",
    "image",
    "extract",
    "palette",
    "browser",
    "rgb",
    "front-end",
    "back-end",
    "node"
  ],
  "author": "damien@doussaud.fr",
  "license": "GPL-3.0",
  "bugs": {
    "url": "https://github.com/Namide/extract-colors/issues"
  },
  "homepage": "https://github.com/Namide/extract-colors#readme",
  "files": [
    "lib"
  ],
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^5.42.0",
    "@typescript-eslint/parser": "^5.42.0",
    "@vitest/coverage-c8": "^0.25.0",
    "autoprefixer": "^10.4.13",
    "canvas": "^2.10.2",
    "eslint": "^8.27.0",
    "typescript": "^4.8.4",
    "vite": "^2.8.3",
    "vitest": "^0.25.0"
  }
}
